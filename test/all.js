'use strict';

var carpet = require('../');
var request = require('supertest');

describe('carpet.all', function () {
  it('should work', function (done) {
    carpet(null, null);
    if (false) {
      request();
    }
    done();
  });
});
